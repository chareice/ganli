.manage
	=button_to "删除帖子",bbs_topic_path(topic),method: :delete,data: {confirm: "确定删除？#{topic.title}"}
	-if topic.hidden?
		=button_to "显示帖子", change_status_bbs_topic_path(topic,status:"public")
	-else topic.public?
		=button_to "隐藏帖子", change_status_bbs_topic_path(topic,status:"hidden")