-left_navs = LeftNav.all
.left-navs-c
  .header
  %ul.left-navs
    -left_navs.each do |nav|
      %li
        %figure
          =link_to image_tag(nav.thumb),nav.url,class: "left-nav-image"
          %figcaption
            =link_to nav.name,nav.url 