.main-page
  .left-c
    -unless current_user
      %div{id:"login-c"}
        =render 'primary_login_form'
    -else
      =render 'welcome'
    .flash-c
      =render 'main_flash'
    =render 'left_navs'
  .right-c
    .row
      .news-1
        =render partial: 'news_1',locals:{classification: 1}
      .news-1
        =render partial: 'news_1',locals:{classification: 2}
    .row.news-1-c
      .news-1
        =render partial: 'news_1',locals:{classification: 6}
      .news-1
        =render partial: 'news_1',locals:{classification: 7}
    .row
      .image-show-c
        =render partial: 'image_show',locals:{classifications:[3,4]}